# To read more about inventory files, see https://pup.pt/bolt-inventory

groups:
  - name: all_hosts
    targets:
      - uri: puppet.pub.rw.betadots.training
        name: puppet

      - uri: puppetdb.pub.rw.betadots.training
        name: puppetdb

      - uri: puppetca.pub.rw.betadots.training
        name: puppetca

      - uri: agent01.pub.rw.betadots.training
        name: agent01

      - uri: monitor01.pub.rw.betadots.training
        name: monitor01

      - uri: monitor02.pub.rw.betadots.training
        name: monitor02

config:
  transport: ssh
  ssh:
    private-key: ~/.ssh/terraform_ed
    user: root
    host-key-check: false
