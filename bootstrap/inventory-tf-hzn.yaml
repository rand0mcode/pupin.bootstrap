# To read more about inventory files, see https://pup.pt/bolt-inventory

groups:
  - name: all_hosts
    targets:
      - uri: puppet.pub.example42.cloud
        name: puppet

      - uri: puppetdb.pub.example42.cloud
        name: puppetdb

      - uri: puppetca.pub.example42.cloud
        name: puppetca

      - uri: agent01.pub.example42.cloud
        name: agent01

      - uri: monitor01.pub.example42.cloud
        name: monitor01

      - uri: monitor02.pub.example42.cloud
        name: monitor02

config:
  transport: ssh
  ssh:
    private-key: ~/.ssh/terraform
    user: root
    host-key-check: false
